import { Repository } from '@/core';
import { {{pluralCamelCase name}} } from '@/db/schema';

import { I{{pascalCase name}} } from './{{kebabCase name}}.interface';

export class {{pascalCase name}}Repository extends Repository<I{{pascalCase name}}> {
  constructor() {
    super({{pluralCamelCase name}});
  }
}